# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

set $mod Mod4

# Remind blue
set $active 2f75c1
set $inactive 696969
set $unfocused 000000

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# tune blue: 007AFF
# tune gray: F0F0F0

#                         border      backgr        text    indicat child_border
# This is the actively focused window
client.focused            #$active    #$active      #ffffff #04859d #2f75c1
# This is the focused window within a tree, but when the tree itself is not focused
client.focused_inactive   #$inactive  #$inactive    #ffffff #04859d #$inactive
client.unfocused          #$unfocused #$unfocused   #ffffff #04859d #$unfocused
client.urgent             #000000     #000000       #F0F0F0 #04859d #000000
client.placeholder        #000000     #000000       #F0F0F0 #04859d #000000
client.background         #000000     #000000       #F0F0F0 #04859d #000000

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:DejaVu Sans Mono 8
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10
default_border pixel
default_floating_border pixel 5
hide_edge_borders smart_no_gaps

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec alacritty
bindsym $mod+q exec --no-startup-id networkmanager_dmenu

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+t focus down
bindsym $mod+n focus up
bindsym $mod+s focus right

# alternatively, you can use the cursor keys:
# bindsym $mod+Left  focus left
# bindsym $mod+Down  focus down
# bindsym $mod+Up    focus up
# bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h  move left
bindsym $mod+Shift+t  move down
bindsym $mod+Shift+n  move up
bindsym $mod+Shift+s  move right

# split in horizontal orientation
bindsym $mod+p split h

# split in vertical orientation
bindsym $mod+y split v

# enter fullscreen mode for the focused container
bindsym $mod+u fullscreen

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# close all windows and exit
# bindsym $mod+Shift+comma workspace $w2; focus parent; kill; workspace $w3; focus parent; kill; workspace $w9; focus parent; kill; workspace $w10; focus parent; kill

# focus the child container
 bindsym $mod+o focus child

# move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# show/hide/cycle scratchpads
bindsym $mod+minus scratchpad show

set $w1 1
set $w2 2
set $w3 3
set $w4 4
set $w5 5
set $w6 6
set $w7 7
set $w8 8
set $w9 9
set $w10 10
set $w11 11
set $w12 12

workspace $w1 output primary
workspace $w2 output primary
workspace $w10 output primary

# switch to workspace
bindsym $mod+1 workspace $w1
bindsym $mod+2 workspace $w2
bindsym $mod+3 workspace $w3
bindsym $mod+4 workspace $w4
bindsym $mod+5 workspace $w5
bindsym $mod+6 workspace $w6
bindsym $mod+7 workspace $w7
bindsym $mod+8 workspace $w8
bindsym $mod+9 workspace $w9
bindsym $mod+0 workspace $w10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1
bindsym $mod+Shift+2 move container to workspace $w2
bindsym $mod+Shift+3 move container to workspace $w3
bindsym $mod+Shift+4 move container to workspace $w4
bindsym $mod+Shift+5 move container to workspace $w5
bindsym $mod+Shift+6 move container to workspace $w6
bindsym $mod+Shift+7 move container to workspace $w7
bindsym $mod+Shift+8 move container to workspace $w8
bindsym $mod+Shift+9 move container to workspace $w9
bindsym $mod+Shift+0 move container to workspace $w10

bindsym $mod+Shift+x move workspace to output right
bindsym $mod+Shift+b move workspace to output up

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# bindsym $mod+Shift+l exec gnome-screensaver-command -l
# bindsym $mod+Shift+l exec i3lock && sudo pm-suspend
bindsym $mod+l exec i3lock -c 000000
#bindsym $mod+l exec slock

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+period exec i3-msg exit
#bindsym $mod+Shift+period exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
  # These bindings trigger as soon as you enter the resize mode

  # Pressing left will shrink the window’s width.
  # Pressing right will grow the window’s width.
  # Pressing up will shrink the window’s height.
  # Pressing down will grow the window’s height.
  # bindsym h resize shrink width 10 px or 10 ppt
  # bindsym t resize grow height 10 px or 10 ppt
  # bindsym n resize shrink height 10 px or 10 ppt
  # bindsym s resize grow width 10 px or 10 ppt

  # same bindings, but for the arrow keys
  bindsym Left resize shrink width 10 px or 10 ppt
  bindsym Down resize grow height 10 px or 10 ppt
  bindsym Up resize shrink height 10 px or 10 ppt
  bindsym Right resize grow width 10 px or 10 ppt

  # back to normal: Enter or Escape
  bindsym Return mode "default"
  bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"
show_marks yes

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#  status_command            i3status
#  position                  bottom
#  #binding_mode_indicator    yes
#  tray_output               primary
#}

bar {
  position top
  status_command i3status-rs ~/.config/i3status-rs/config.toml
  tray_padding 4px
  workspace_buttons yes
  binding_mode_indicator yes
  font pango:DejaVu Sans Mono 10, FontAwesome 12
}

focus_follows_mouse no
focus_on_window_activation urgent
mouse_warping output
workspace_auto_back_and_forth yes

# Backlight control
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5

# Audio control
bindsym XF86AudioRaiseVolume exec pamixer -i 5 && pkill -RTMIN+1 i3blocks
bindsym XF86AudioLowerVolume exec pamixer -d 5 && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute exec pamixer --get-mute && pamixer --unmute || pamixer -m && pkill -RTMIN+1 i3blocks

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Marks
bindsym $mod+Shift+F1 mark F1
bindsym $mod+Shift+F2 mark F2
bindsym $mod+Shift+F3 mark F3
bindsym $mod+Shift+F4 mark F4
bindsym $mod+Shift+F5 mark F5
bindsym $mod+Shift+F6 mark F6
bindsym $mod+Shift+F7 mark F7
bindsym $mod+Shift+F8 mark F8
bindsym $mod+Shift+F9 mark F9
bindsym $mod+Shift+F10 mark F10
bindsym $mod+Shift+F11 mark F11
bindsym $mod+Shift+F12 mark F12

bindsym $mod+F1 [con_mark="F1"] focus
bindsym $mod+F2 [con_mark="F2"] focus
bindsym $mod+F3 [con_mark="F3"] focus
bindsym $mod+F4 [con_mark="F4"] focus
bindsym $mod+F5 [con_mark="F5"] focus
bindsym $mod+F6 [con_mark="F6"] focus
bindsym $mod+F7 [con_mark="F7"] focus
bindsym $mod+F8 [con_mark="F8"] focus
bindsym $mod+F9 [con_mark="F9"] focus
bindsym $mod+F10 [con_mark="F10"] focus
bindsym $mod+F11 [con_mark="F11"] focus
bindsym $mod+F12 [con_mark="F12"] focus

# Automatically apply transforms to slack and ripcord. You can find classes by
# using `xdotool search --class <class>`
#for_window [class="(?i)slack|ripcord"] floating enable, move scratchpad
#resize set 1800 1012, move position 50 50,
assign [class="(?i)zoom"] $w10

gaps horizontal 3
smart_gaps on

bindsym $mod+Shift+f floating enable, resize set 5120 1440, move absolute position center
